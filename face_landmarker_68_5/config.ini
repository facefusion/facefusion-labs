[datasets]
dataset_path = /dataset

[embeddings]
source_path = embeddings/landmark_source.npy
target_path = embeddings/landmark_target.npy

[outputs]
directory_path = outputs
file_name = landmark-converter-{epoch:02d}-{val_loss:.4f}

[execution]
device = cuda

[training]
batch_size = 512
max_epochs = 5000
